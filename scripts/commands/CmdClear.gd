"""
ตัวอย่างการเพิ่ม Command
"""
extends Command

func _init():
	var key = "สวัสดีโลก" # เมื่อพิมพ์ข้อความนี้ โค้ดจะทำงาน

func fun(): # ชุดโค้ดที่จะะทำงานเมื่อพิมพ์คำสั่ง
	game.printf("สวัสดีมนุษย์") # แสดงผลข้อความนี้ในเกม
